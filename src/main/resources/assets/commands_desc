/sf distribution captain start [--use_existing_teams] [--use_existing_captain]
    Запускает распределение
    Флаг "--use_existing_teams" запускает распределение с существующими командами.
    Флаг "--use_existing_captain" запускает распределение с существующими копитанами.
        Существующий капитан - это игрок зашедший первым в команду или установленный администратором

/sf distribution captain cancel [--save_teams]
    Заканчивает распределение (Досрочно)
        Зачем это нужно? Пример:
            Началось распределение, ты пишешь две команды:
                /sf distribution captain cancel --save_teams
                /sf distribution captain start --use_existing_teams
            Распределение продолжится с новым игроком. (Возможно стоит добавить новую команду, например, /sf distribution captain addPlayer <nickname>)
    Флаг "--save_teams" заканчивает распределение и сохраняет текущие команды (Например, если капитаны выбрали несколько человек, то капитан и игроки попадут в комнату команд, а не в лобби)

/sf distribution captain info
    Показывает информацию о методе получения капитана для распределения

/sf distribution captain reset
    Сбрасывает всех капитанов, устанавливая метод получения капитана "random"

/sf distribution captain set <red, green, blue, yellow> [--disabled, --random, --player=<nickname>]
    Устанавливает капитана (или способ получения капитана) для определенной команды
    Флаг "--disabled" отключает команду для распределения
    Флаг "--random" выберет случайного игрока (во время старта распределения), как капитана
    Флаг "--player=<nickname>" установит необходимого игрока, как капитана

/sf distribution captain setup [--disabled, *--random]
    Устанавливает капитанов для распределения, источниками становятся игроки которые были в комнатах
    Подкоманда "disabled" отключает команду для распределения, если в комнате не было игроков
    Подкоманда "random" выберет случайного игрока(во время старта распределения), если в комнате не было игроков

<> -> Обязательный параметр
[] -> Опциональный параметр
*  -> Используется по умолчанию